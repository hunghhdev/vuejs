const state = () => ({
    posts:[{id: 1, title: "hello nuxt"}]
})
const getters = {};
const actions = {};
const mutations = {};

const postModule = {
    state,
    getters,
    actions,
    mutations
}

export default postModule
